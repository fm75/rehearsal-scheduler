# Rehearsal Directors constraints
# Uses constraints grammar

name: rd_constraints

columns:
  - rd_id
  - full_name
  - constraints
  - ai_translation
  - raw_constraints_from_form

protected_rows:
  - 1

protected_columns:
  - rd_id
  - full_name
  
formulas:
  full_name: 'VLOOKUP(rd_id,rehearsal_directors!A:D,4,FALSE)'
  
auto_id_config:
  column: rd_id
  prefix: 'rd_'
  count: 10
